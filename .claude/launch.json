{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "streamlit",
      "runtimeExecutable": "streamlit",
      "runtimeArgs": ["run", "app.py", "--logger.level=error", "--server.port=${PORT}"],
      "port": 8501,
      "autoPort": true
    }
  ]
}
